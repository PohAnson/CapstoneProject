"""
Configuration files for variables.

db_path: path of the database
host: the ip address of host
port: the port number of host for flask
"""

# Data Storage
db_path = "src/datastore/database.db"
graph_path = "src/graph.json"

# Website
host = "0.0.0.0"
port = "5000"
